name: 🚧 Deploy to stg.alvin.id
on:
  push:
    branches:
      - staging

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: ✅ Deployment triggered via webhook
        run: |
          echo "Push to staging detected."
          echo "GitHub will fire a webhook to https://stg.alvin.id/deploy.php"
          echo "The server will pull and deploy the latest code automatically."
